# 工单8 双层意图识别 周期评测报告

样本数：**110**（模板增强自动生成，gold 确定）
规则命中率：**81.8%**

## 验收红线
- 意图召回 Recall：**100.0%**（红线 ≥ 90%）
- 幻觉率：**0.0%**（红线 ≤ 2%）
- Token 降幅（混合 vs 纯 LLM）：**72.5%**（红线 ≥ 40%）
- TTFT 降幅：**81.8%**

## A/B 对比（hybrid = 规则+LLM / pure_llm = 仅 LLM）

| 指标 | 混合流 | 纯 LLM |
|------|--------|--------|
| 召回 Recall | 100.0% | 95.5% |
| 幻觉率 | 0.0% | 4.5% |
| LLM 调用次数 | 20 | 110 |
| 估算 Token | 142 | 516 |
| 平均 TTFT(ms) | 145.5 | 800.0 |

> 说明：离线基准用 StubLLMClient 模拟真实 LLM（acc=0.95）；TTFT 为建模值（规则命中率 × 配置 LLM 延迟），不真实 sleep。真实环境以 `use_real_llm=True` 替换。

## 基线回归

- 相对基线无劣化，全部红线稳定。

## 红线达标 ✅

全部验收红线（Recall≥90% / 幻觉≤2% / Token降≥40%）通过。

## 决策 Golden 评测（mock）

详见 `docs/评测报告.md`（已刷新）。

---

生成时间：2026-09-08T16:28:22