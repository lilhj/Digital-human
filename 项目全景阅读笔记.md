# 电商购买与 Agent 协同退款平台 · 项目全景阅读笔记

> 阅读时间：2026-09-22 ｜ 范围：README + 工作流代码 + 安全模块 + 意图模块 + 需求文档 + 数字人模块 + 评测报告 + 前端结构
> 一句话：一套买家可真实下单/支付/售后的电商演示平台，后端用 **9 个 Agent 节点串行编排**售后退赔全流程，并叠加「零信任安全网关 + 双层意图 + 订单三查 + 数字人直播」。

---

## 1. 系统定位（专业拆解）

平台 = 轻量电商前台（造真实订单）+ 9 节点智能退赔决策流（处理售后）+ 安全网关（防注入/防资损）+ 数字人卖货直播入口。

- 后端：FastAPI + SQLAlchemy 2.0 + PostgreSQL + Redis Streams + LangGraph + PaddleOCR（可降级）。
- 前端：React 18 + TypeScript + Vite（端口 5173）。
- 异步：Redis Streams 生产者-消费者，Worker 独立进程消费。
- 可观测：Langfuse spool（落盘 + 后台 flush，非阻塞降级）。

**通俗类比**：把"售后客服 + 风控 + 主管审核"整条链路做成一条工厂流水线。包裹（售后申请）从 A 工位流到 I 工位，每个工位只干一件事，干完盖章（checkpoint）再传给下一位；一旦某工位发现是假货或脏数据，直接短路甩给主管，不浪费后面贵重机器（LLM）的算力。

---

## 2. 架构分层

```
前端层(React) ──▶ 接口层(FastAPI, JWT 双身份) ──▶ 编排层(LangGraph 9节点 + 安全网关)
                                                          │
                                              ┌───────────┼────────────┐
                                          异步层(Redis)   数据层(PG/Redis)   可观测(Langfuse)
                                              │
                                          Worker 进程消费
```

- 双身份鉴权：买家 JWT（`type=BUYER`）与员工 JWT（`type=STAFF`）完全隔离。
- 统一错误格式 `{"code","message"}`，兜底 500 不泄露堆栈。

---

## 3. 9 节点工作流（核心，全串行，非并行）

代码：`backend/app/workflow/graph.py`（路由）+ `nodes.py`（9 节点实现）

| # | 节点 | 职责 | 关键产物 |
|---|---|---|---|
| ① | INTAKE | 读案件 + Critic 语义安检 + DLP 脱敏（内联 CRITIC/DLP 两个子 AgentRun） | `security_action`, `security_risk_score` |
| ② | INTENT | 双层意图（规则/LLM）+ 用户显式选择交叉校验 | `intent`, `intent_fallback` |
| ③ | ORDER_VERIFY | 订单三查硬闸（真实性/一致性/防重复） | `order_verify_result` |
| ④ | EVIDENCE | 凭证图 OCR + 视觉理解（Qwen2.5-VL，可降级） | `evidence_text`, `vision_*` |
| ⑤ | FRAUD | 风控+舆情合并为一次 LLM 调用 | `fraud_score`, `sentiment_score`, `risk_level` |
| ⑥ | SENTIMENT | 直接消费 ⑤ 结果（零额外 LLM 调用） | 复用 state |
| ⑦ | DECISION | 阈值/规则决策 → APPROVE/REJECT/HUMAN_REVIEW | `decision` |
| ⑧ | HUMAN_REVIEW | 置 SUSPENDED + 建 ReviewTask + LeastActive 派单 + `interrupt()` 挂起 | 人工裁决回填 |
| ⑨ | FINALIZE | 写退款/完结案件 + 回写订单状态 | 终态落库 |

**三条真实路径**
- A 正常退款：②退款→③PASS→④OCR→⑤风控→⑦APPROVE→⑨结算（全自动）
- B 伪造/重复：③REJECT→⑦REJECT→⑨结算（跳过 OCR/风控 LLM，直接拒）
- C 换货/冲突：②fallback→⑧人工复核挂起（跳过决策与全部 LLM）

**为什么串行**：数据链路天然依赖（OCR 文本喂风控、风险分喂决策）；硬闸先验后验"省钱且防误判"；安全排序即安全（Critic 在任何 LLM 之前）；确定性强、好审计/好断点恢复。

---

## 4. 安全设计（工单6，零信任）

代码：`backend/app/security/critic.py` + `dlp.py` + `tool_filter.py`

- **Critic 语义安检**（规则+启发式双引擎，零 LLM 依赖，常驻）：注入/越权类单条即 BLOCK（如"直接退款""跳过人工"权重 0.85~0.95）；累加 ≥0.85 触发 BLOCK。覆盖中/英/日/西/韩跨语言高频词。BLOCK 后转人工复核（不直接拒，防误伤）。
- **DLP 两处脱敏**：进 LLM 前 + 写日志前。手机号/身份证/API Key/银行卡/邮箱正则遮掩，准确率 ≥99%。注意中文相邻边界用否定环视兜底（避免 `\b` 漏脱敏）。
- **Tool 过滤**：finalize 执行退款前再卡一道（Critic 漏拦的边缘注入）。
- **纵深**：图片 VL 描述也过 Critic（图内注入指令拦截）；ocr_text/evidence 落库前脱敏；Redis 挂起快照写入前脱敏；AgentRun 日志不存敏感原文。

**已知缺口（红蓝报告）**：语义/变形类攻击（空格分词、同义改写、无关键词隐含指令）规则引擎覆盖不到（漏拦 3/133）；DLP 仅正则，对带分隔符身份证需补 NER。建议接轻量语义模型做二分类兜底，红蓝对抗纳入 CI。

---

## 5. 双层意图识别（工单8）

代码：`backend/app/intent/classifier.py`

- Node A 规则层：明确关键词直接判（退款/退货/换货），零 LLM、零延迟。
- Node B LLM 层：口语化/模糊表述才精判，严格 JSON 输出；失败/超时/UNKNOWN 兜底转人工（**不因模型失败自动放行**，资损零容忍）。
- 交叉校验 `apply_declared_claim`：买家端显式三选一（claim_type）为主通道——一致采信、UNKNOWN 采信用户选择、冲突转人工。
- 路由：EXCHANGE/UNKNOWN/冲突 → `intent_fallback=True` → 人工复核（MVP 不自动处理换货）。

**通俗类比**：规则层是"老员工秒回标准问题"，LLM 是"遇到绕弯的话才请专家"，用户自己点的卡片是"最终确认"——三道都拿不准就交给主管，绝不让机器瞎批退款。

---

## 6. 订单三查 + 防重复退款 + 回写闭环（v2.0）

- **三查硬闸**：订单不真实 → REJECT（伪造）；商品/价格不一致 → HUMAN_REVIEW；申报额 > 实付额 / 对账不平 → 转人工或拒。
- **防重复退款**：`order_verify` 顺带查 `Order.status ∈ {REFUNDED,REFUNDING,EXCHANGING,CLOSED}` → REJECT；finalize 前还有 `_order_already_refunded_elsewhere` 兜底（同订单已被其他案件退款完成则拒）。三层防重：幂等键 + 分布式锁 + 状态校验。
- **回写闭环**：退款通过后 `finalize_node` 同步回写 `Order.status=REFUNDED`（与 checkpoint 同事务，宁耦合不可劈叉）。

---

## 7. 电商前台（5 表 + 8 态状态机）

代码：`backend/app/api/` 下 `customer_auth/products/cart/orders`（并排 cases.py）

- 5 表：Customer / Product / CartItem / Order / OrderItem（金额一律「分」BIGINT，禁浮点；PBKDF2 哈希密码）。
- 8 态：PENDING_PAYMENT→PAID→SHIPPED→COMPLETED，售后支线 REFUNDING→REFUNDED / EXCHANGING / CLOSED。前端归并为三态主线（运行中/挂起中/已完成）。
- 商品数据：小米商城爬虫，17 分类 392 商品 + 392 图（`products/` 目录），seed 导入。
- 挂起上下文双写：PG checkpoint（权威真相）+ Redis JSON 快照（TTL 7天，加速展示/导出），Redis 不可用仅告警不阻断。

---

## 8. 数字人直播模块

文档：`数字人直播/电商数字人直播_开发与落地操作文档_V1.0.md` + `阶段1_部署架构规格.md`

- 核心思想：**数字人负责"身体+声音"，大模型负责"大脑"，业务系统负责"商品/库存/订单/直播控制"**。
- 竞品路线参考：腾讯云数智人 / HeyGen LiveAvatar / 美团智播 / 魔珐有光 → 抽象出"把数字人和 LLM 解耦、统一 Provider 接口"的结论。
- 直播 Agent 设计：弹幕→意图识别→Director Agent 调度（主播/商品/互动 Agent）→工具调用（get_product/stock/price/coupon/add_to_cart）→TTS→数字人。强调"主动讲商品 + 支持打断恢复 + 价格库存不写死 Prompt、实时查 API"。
- **阶段1 部署定案（已确认）**：重算力（LLM 中转 / TTS / 口型引擎 / SRS）上算力云 GPU（起步 RTX 4090 24G，AutoDL 按量 ¥2-3/h）；本地 RTX 5060 只留 React 前端 + `/api/live` 路由 + 弹幕中继。后端栈锁定 LiveTalking + MuseTalk/Ultralight；对话大脑恒定 agicto 中转 deepseek-v4-flash（¥2/¥8 per M tokens）。环境必须 CUDA 12.4 + PyTorch 2.5+（工单21 写的 CUDA11.7 不兼容 Blackwell）。

---

## 9. 评测体系（结果全绿/达标）

| 评测 | 规模 | 关键结果 |
|---|---|---|
| Golden（mock 模式） | 10 用例 | 正确性5.0/安全性5.0/理由3.0（综合4.33） |
| Golden（real 模式） | 10 用例 | 三维全 5.0（综合5.0） |
| 意图周期测试 | 110 样本 | 召回100%、幻觉0%、Token↓72.5% |
| 红蓝对抗 | 133 攻击样本 | 注入拦截100%、越狱100%、DLP漏报0% |
| 客服知识库 RAG 回归 | 20 条 | 五维全绿 |
| 单元/集成测试 | 30 文件 | 全绿 |

---

## 10. 关键决策与权衡（速查）

- 串行不并行：把 ⑤⑥ 合并成一次 LLM 调用（Token↓40~50%、时延砍半），而非并行两次调用。
- 安全拦截转人工不直拒：防误伤，且 BLOCK 是最高优先级定性（共享 review_reason 槽位统一自证文案）。
- 支付/回写同步非异步：资损敏感，宁耦合不可劈叉（钱退了订单没标记=事故）。
- PG 主存 + Redis 快照双写：纯 Redis 无 AOF 一重启就"失忆"，退赔不能丢。
- 金额一律「分」整数：禁浮点。
- 退款失败兜底 `_force_refund_failed`：绝不停在 APPROVED 模糊态（M-2 修复）。

---

## 11. 已知缺口 / 风险（值得关注）

1. 安全：语义/变形类注入规则引擎漏拦（3/133）；DLP 仅正则，结构化 PII 变体需 NER。
2. 客服知识库：运费险状态/价保入口/发票/花呗分期等被引用但系统未实现（v1.0 已知缺口清单）。
3. 数字人：MVP 尚未打通（仅设计+阶段1部署规格），本地 5060 显存不足需上云；依赖第三方 Avatar 供应商 SDK 抽象层。
4. 评测：理由质量在 mock 模式仅 3.0（缺 LLM 真实打分理由），real 模式才到 5.0——依赖真实 Provider 注入。

---

## 12. 关键文件导航（想改哪看哪）

| 想做的事 | 看这个文件 |
|---|---|
| 改工作流路由/串行逻辑 | `backend/app/workflow/graph.py` |
| 改 9 节点实现/退款/挂起 | `backend/app/workflow/nodes.py` |
| 改安全拦截规则/脱敏 | `backend/app/security/critic.py` `dlp.py` `tool_filter.py` |
| 改意图识别 | `backend/app/intent/classifier.py` |
| 改决策阈值 | `backend/app/policy/decision.py` |
| 改订单三查 | `backend/app/workflow/order_verify.py` |
| 加接口 | `backend/app/api/*.py` |
| 改入口/路由挂载 | `backend/app/main.py` |
| 看需求 | `docs/需求文档-电商平台v2.0.md` `需求文档-客诉退赔v4.0.md` |
| 看数字人方案 | `数字人直播/电商数字人直播_开发与落地操作文档_V1.0.md` `阶段1_部署架构规格.md` |
| 看评测 | `docs/评测报告.md` `评测报告_real.md` `red_blue_test_report.md` |
| 看客服语料 | `docs/客服知识库.md` |

---

*注：frontend 含 3183 文件（React 工程），src 顶层为 App.tsx / api / components / layouts / pages / theme.ts；buyer 侧 7 页面挂在 `/buyer/*`。完整逐文件阅读未覆盖（体量过大），以上为架构+核心逻辑+文档层面全量阅读。*
